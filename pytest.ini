[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    -ra
    --strict-markers
    --cov=scripts
    --cov-config=.coveragerc
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=80
